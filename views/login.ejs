<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
  <div class="min-h-screen flex items-center justify-center p-6">

    <section class="w-full max-w-md bg-zinc-800 p-8 rounded-lg shadow-md">
      <h3 class="text-3xl font-semibold mb-6 text-center">Login</h3>
      <form method="POST" action="/login" class="space-y-4">
        <input type="text" name="email" placeholder="Email" class="w-full px-3 py-2 rounded-md bg-transparent border border-zinc-700 outline-none" />
        <input type="password" name="password" placeholder="Password" class="w-full px-3 py-2 rounded-md bg-transparent border border-zinc-700 outline-none" />
        <input type="submit" value="Login" class="w-full bg-green-500 py-2 rounded-md hover:bg-green-600 cursor-pointer" />
      </form>

      <p class="text-center mt-4 text-sm">
        Don't have an account?
        <a href="/" class="text-blue-400 underline hover:text-blue-300">Create Account</a>
      </p>
    </section>

  </div>
</body>
</html>
